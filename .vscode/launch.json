{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "EV 내비 서버 실행 (uvicorn)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["backend.app.main:app", "--reload", "--port", "8000"],
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    }
  ]
}
